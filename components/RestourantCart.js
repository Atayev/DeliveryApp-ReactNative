import { View, Text } from 'react-native'
import React from 'react'

const RestourantCart = ({
    id,
    imgUrl,
    title,
    rating,
    genre,
    address,
    short_descr,
    dishes,
    long,
    lat
}) => {
  return (
    <View>
      <Text>RestourantCart</Text>
    </View>
  )
}

export default RestourantCart